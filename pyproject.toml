[project]
name = "trinity-dev"
version = "0.0.0"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "coverage[toml]",
    "pytest",
    "pytest-asyncio",
    "pytest-bdd>=8.1.0,<9",
    "pytest-cov",
    "ruff",
]

[tool.uv]
package = false
